import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  termsText: {
    color: '#FF6666',
    fontFamily: 'Evogria',
    fontSize: 15,
    marginBottom: 30,
    marginTop: 30,
  },
})

export default styles
